import React from 'react';
import Button from './button'
import { Loader } from './loader';

import '../App.css';

const App = () => {
	return (
		<div className='childDiv'>
			<p>
				Edit <code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla Edit{' '}
				<code>src/App.js</code> and save to reload.alsd,dla
			</p>
		</div>
	);
};

export default App